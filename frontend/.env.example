# Base URL of the FastAPI backend (include the /api prefix).
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000/api
# Production example:
# NEXT_PUBLIC_API_URL=https://calimuv-api.onrender.com/api

# Public site URL (used for SEO metadata, sitemap, canonical URLs).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics (added in Step 11; leave blank for now).
NEXT_PUBLIC_GA_ID=
